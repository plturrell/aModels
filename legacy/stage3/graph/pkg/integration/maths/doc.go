// Package maths wires LangGraph-Go execution primitives into the agenticAiETH
// Layer1 maths infrastructure. The integration exposes context-aware wrappers
// around the high-performance tensor and linear algebra utilities so the Go
// runtime can depend on them without tight coupling.
package maths
