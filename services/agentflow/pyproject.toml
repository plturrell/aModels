[project]
name = "agentflow"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlmodel",
    "httpx",
    "redis",
    "python-dotenv",
    "pydantic-settings",
]

[project.optional-dependencies]
postgresql = ["psycopg2-binary"]
