simple-ddl-parser==1.7.1
# DeepSeek OCR dependencies (for USE_DEEPSEEK_OCR=true)
# Now using Python base image, so PyTorch can be installed
transformers>=4.30.0
torch>=2.0.0
accelerate>=0.20.0
safetensors>=0.3.0
huggingface-hub>=0.16.0
Pillow>=9.0.0
pandas>=1.5.0
numpy>=1.24.0
